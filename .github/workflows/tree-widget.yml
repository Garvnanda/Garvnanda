name: Update ecosystem tree

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Generate tree.svg
        run: node scripts/generate-tree-svg.mjs
        env:
          GH_USERNAME: Garvnanda
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit if changed
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git add assets/tree.svg assets/dark/tree.svg
          git diff --cached --quiet && exit 0
          AUTHOR="$(bash scripts/pick-author.sh)"
          git config user.name "${AUTHOR%%|*}"
          git config user.email "${AUTHOR##*|}"
          git commit -m "chore: refresh ecosystem tree"
          for i in 1 2 3 4 5; do
            git pull --rebase origin main && git push && break
            sleep $((RANDOM % 10 + 1))
          done
